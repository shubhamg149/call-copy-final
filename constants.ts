
// MOCK_USERS and MOCK_ANALYSIS_HISTORY are removed as user data and call reports will now be fetched from Firebase Firestore.

// Removed the long base64 string to prevent SyntaxError. 
// Testing can be done with small user-uploaded files.
export const SAMPLE_AUDIO_BASE64 = "";